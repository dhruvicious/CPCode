#include <bits/stdc++.h>
using namespace std;

constexpr long long MOD = 10000000007LL;

int32_t main() {
#ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
#endif

    int n;
    cin >> n;
    vector<int> arr(n);

    for (int& i : arr) cin >> i;

    return 0;
}
#include <iostream>
#include <vector>

using namespace std;

class Solution {
   public:
    long long maximumScore(vector<vector<int>>& grid) {}
};

int main() {
    int n;
    cin >> n;
    vector<vector<int>> arr(n, vector<int>(n));
}
